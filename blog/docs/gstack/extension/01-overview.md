# 扩展结构与构建

> **状态**：导读骨架。

阅读建议：从扩展的 **manifest** 与 **入口脚本** 开始，理清 side panel / background / content script 边界，再对照 `extension/` 子目录中的 UI 与消息协议。

构建与加载方式以仓库 README、CLAUDE.md 及 `package.json` 脚本为准。

返回 [extension 模块首页](/gstack/extension/)。
